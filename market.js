const Market={candles:[],price:null,prev:null,timeframe:"M5",generate(n=100){let p=4300+Math.random()*30;this.candles=[];const step={M1:60000,M5:300000,M15:900000,H1:3600000}[this.timeframe]||300000;let t=Date.now()-n*step;for(let i=0;i<n;i++){let drift=(Math.random()-.49)*5;let o=p,c=Math.max(1,o+drift),h=Math.max(o,c)+Math.random()*3,l=Math.min(o,c)-Math.random()*3;this.candles.push({t,o,h,l,c});p=c}this.prev=this.candles.at(-2)?.c||p;this.price=this.candles.at(-1).c},setTimeframe(tf){this.timeframe=tf;this.generate()},valid(){return this.candles.length>=30&&this.candles.every(x=>num(x.o)&&num(x.h)&&num(x.l)&&num(x.c)&&x.h>=Math.max(x.o,x.c)&&x.l<=Math.min(x.o,x.c))},latest(){return this.candles.at(-1)}};Market.generate();
