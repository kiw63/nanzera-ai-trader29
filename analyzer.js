function sma(a,n){if(a.length<n)return null;return a.slice(-n).reduce((x,y)=>x+y.c,0)/n}function momentum(c,n=8){if(c.length<=n)return 0;return c.at(-1).c-c.at(-1-n).c}const Analyzer={run(){if(!Market.valid())return {signal:"WAIT",score:0,trend:"UNKNOWN",structure:"UNKNOWN",momentum:"UNKNOWN",reasons:["Market data tidak valid atau kurang." ]};const c=Market.candles, p=Market.price, fast=sma(c,10),slow=sma(c,30),mom=momentum(c);let trend=fast>slow?"BULLISH":fast<slow?"BEARISH":"SIDEWAYS";let structure=c.at(-1).c>c.at(-6).c?"HH/HL BIAS":"LH/LL BIAS";let mo=mom>0?"POSITIVE":mom<0?"NEGATIVE":"FLAT";let score=50,reasons=[];if(trend==="BULLISH"){score+=15;reasons.push("MA10 berada di atas MA30.")}else if(trend==="BEARISH"){score+=15;reasons.push("MA10 berada di bawah MA30.")}if((trend==="BULLISH"&&mom>0)||(trend==="BEARISH"&&mom<0)){score+=15;reasons.push("Momentum searah dengan trend.")}else{score-=10;reasons.push("Momentum belum searah.")}if(Math.abs(mom)>2){score+=5;reasons.push("Pergerakan harga cukup aktif.")}else reasons.push("Momentum relatif lemah.");let signal=score>=NANZERA_CONFIG.minScore?(trend==="BULLISH"?"BUY":"SELL"):"WAIT";if(trend==="SIDEWAYS")signal="WAIT";return{signal,score:clamp(score,0,100),trend,structure,momentum:mo,reasons,entry:p}}}
