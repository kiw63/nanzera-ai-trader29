const Backtest={run(){const c=Market.candles;if(c.length<30)return null;let bal=10000,wins=0,losses=0,gp=0,gl=0;for(let i=30;i<c.length-1;i++){const fast=c.slice(i-10,i).reduce((s,x)=>s+x.c,0)/10,slow=c.slice(i-30,i).reduce((s,x)=>s+x.c,0)/30;let side=fast>slow?"BUY":fast<slow?"SELL":null;if(!side)continue;const entry=c[i].c,next=c[i+1].c,pl=side==="BUY"?next-entry:entry-next;bal+=pl;if(pl>0){wins++;gp+=pl}else{losses++;gl+=-pl}}const trades=wins+losses;return{trades,wins,losses,winRate:trades?wins/trades*100:0,profitFactor:gl?gp/gl:0,net:bal-10000,balance:bal}}};
